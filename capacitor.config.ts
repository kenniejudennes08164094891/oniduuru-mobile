import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', //com.shoftafrica.oniduuru
  appName: 'oniduuru-marketplace',
  webDir: 'www',
};

export default config;
